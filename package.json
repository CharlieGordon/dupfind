{
  "name": "duplicate-file-finder",
  "version": "1.0.0",
  "description": "Node.js CLI to find duplicate files by content hash",
  "main": "dist/index.js",
  "bin": {
    "dupfind": "dist/index.js"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "typescript": "^5.4.5",
    "vitest": "^1.2.0"
  }
}
